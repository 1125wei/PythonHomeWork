"""
依照格式打印进度条
"""
load = 11  # 循环次数
print("——————————执行开始——————————")
for i in range(0, load):  # 循环10次
    print("%" + "{:>3d} " .format(10 * i) + "[" + "**" * i + "->" + ".." * (10 - i) + "]")  # 多次输出，
print("——————————执行结束——————————")
